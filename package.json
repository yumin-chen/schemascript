{
	"name": "artefact",
	"module": "src/main.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"fix": "bun run format && bun run check && bun run lint",
		"check": "bun x @biomejs/biome check --fix .",
		"format": "bun x @biomejs/biome format --fix .",
		"lint": "bun x @biomejs/biome lint --fix .",
		"build:sql": "bun --define BUILD_TARGET='\"SQLite\"' x drizzle-kit generate --dialect sqlite --schema ./src/data/schemas/**/*.schema.ts",
		"start": "bun run src/main.ts"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"drizzle-kit": "^0.31.9"
	},
	"peerDependencies": {
		"typescript": "^5.9.3"
	},
	"dependencies": {
		"drizzle-orm": "0.45.1"
	}
}
